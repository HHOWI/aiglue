tools_yaml_version: "1.0"
tools:
  - name: delete_item
    description: "아이템을 삭제한다"
    endpoint: DELETE /api/items/:id
    params:
      id:
        description: "아이템 ID"
        required: true
    risk_level: critical
